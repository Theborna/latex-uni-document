\usepackage{graphicx}
\usepackage{xcolor}
\usepackage{lipsum}
\usepackage[a4paper,margin=1in]{geometry}
\usepackage{tikz}
\usepackage{lastpage}
\usetikzlibrary{calc}
%\usetikzlibrary{patterns}
\usepackage[
  automark,
  autooneside=false,% <- needed if you want to use \leftmark and \rightmark in a onesided document
  headsepline,
  footsepline
]{scrlayer-scrpage}
\usepackage{calc}
\newcommand{\sigline}[1]{\makebox[\widthof{#1~}]{.\dotfill}}
\newcommand{\defInfo}{
  \renewcommand{\title}{\sigline{موضوع فایل}}
  \renewcommand{\author}{\sigline{نام استاد}}
  \newcommand{\department}{دانشکده: \sigline{نام دانشکده}}
  \renewcommand{\subject}{تمرین شماره: \dots}
  \newcommand{\info}{نویسنده: \sigline{نام نویسنده}}
}
\newcommand{\setInfo}[5]{
  \renewcommand{\title}{#1}
  \renewcommand{\author}{#2}
  \newcommand{\department}{#3}
  \renewcommand{\subject}{#4}
  \newcommand{\info}{#5}
}

\newcommand{\myborder}{%
  \begin{tikzpicture}[remember picture,overlay]
    \clip (current page.north west) rectangle ($(current page.north east)+(0,-0.7)$);
    \foreach \x in {-9,-6.3,...,27}{
      \draw[red, line width=0.6cm, rotate=-45] (\x,-\paperheight) -- ++(0,2*\paperheight);
      \draw[blue!30!lightgray,line width=0.6cm, rotate=-45] (\x+1.35,-\paperheight) -- ++(0,2*\paperheight);
    }
  \end{tikzpicture}
}
\newcommand{\natije}[1]{
    % \vspace*{1em}\\
    \rule{\textwidth}{0.5pt}
    \begin{abstract}
        \centering
        #1
    \end{abstract}
    \rule{\textwidth}{0.5pt}
}
\ihead*{\Ifstr{\leftmark}{\rightbotmark}{\title}{\rightbotmark}}
\ohead{\Ifstr{\leftmark}{\rightbotmark}{\subject}{\rightbotmark}}
\cfoot*{\pagemark-\pageref{LastPage}}
% \renewcommand*\chapterpagestyle{scrheadings}